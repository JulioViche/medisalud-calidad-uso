# Scripts heredados

Estos scripts se mantienen como antecedente y no forman parte del flujo vigente. Mezclan generación, cálculo y exportación y utilizan el periodo enero-febrero de 2025.

El pipeline actual se ejecuta con:

```bash
python -m analytics.exporters.pipeline --seed 25022
```

